@lxpanel --profile LXDE-pi
@pcmanfm --desktop --profile LXDE-pi
@xscreensaver -no-splash

# Screen power management (画面を消さない)
@xset s off
@xset -dpms
@xset s noblank

# Hide mouse cursor (optional: uncomment if needed)
# @unclutter -idle 0.1 -root

# Start Streamlit App
# ※パスは /home/pi/BrainBridge を前提としています
@bash -c "source /home/pi/BrainBridge/app/venv/bin/activate && cd /home/pi/BrainBridge/app && streamlit run main.py --server.port 8501 --server.headless true"

# Start Chromium in Kiosk Mode
# サーバー起動待ち(20秒)の後にブラウザを起動
@bash -c "sleep 20 && chromium-browser --kiosk --incognito http://localhost:8501"