@lxpanel --profile LXDE-pi
@pcmanfm --desktop --profile LXDE-pi
@xscreensaver -no-splash

# 画面を消さない設定
@xset s off
@xset -dpms
@xset s noblank

# マウスカーソルを非表示にする
@unclutter -idle 0.1 -root

# BrainBridge起動スクリプトを実行
# ターミナルを出さずに裏で実行させる
@bash /home/pi/brainbridge/run.sh